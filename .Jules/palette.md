## 2024-05-21 - [API Root Status Page]
**Learning:** Developer-facing API services benefit significantly from a visual "System Operational" root page. It provides immediate feedback without needing tools like Postman or curl.
**Action:** For API-first projects, always implement a lightweight `GET /` HTML status page using `system-ui` fonts and dark mode for developer comfort.
