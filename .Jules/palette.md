## 2026-01-31 - Missing Status Page
**Learning:** API-only services often lack a root status page, leaving users (and developers) unsure if the service is running or if they have the wrong URL. A simple "System Operational" page provides immediate, delightful feedback.
**Action:** Always verify if an API service has a default handler for `/` and add a lightweight status page if missing.
